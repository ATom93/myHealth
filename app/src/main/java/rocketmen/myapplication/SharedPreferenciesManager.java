package rocketmen.myapplication;

import android.content.SharedPreferences;

import static android.content.Context.MODE_PRIVATE;

/**
 * Created by luigi on 06/12/2018.
 */

public class SharedPreferenciesManager {

    /*
    private static final String MY_PREFS_NAME = "";

    public void salva_sharedPreferencies(String nome_variabile,String variabile){
        SharedPreferences.Editor editor = getSharedPreferences(MY_PREFS_NAME, MODE_PRIVATE).edit();
        editor.putString(nome_variabile, variabile);
        editor.apply();

    }

    public String leggi_sharedPreferencies(String nome_variabile){
        String name="null";
        SharedPreferences prefs = getSharedPreferences(MY_PREFS_NAME, MODE_PRIVATE);
        String restoredText = prefs.getString(nome_variabile, null);
        if (restoredText != null) {
            name = prefs.getString(nome_variabile, "No name defined");//"No name defined" is the default value.
        }

        return name;

    }*/

}
